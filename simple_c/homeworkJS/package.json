{
  "name": "simple_c",
  "version": "1.0.0",
  "description": "try:    ``` make run ```",
  "main": "index.js",
  "scripts": {
    "watch": "nodemon -q -x 'make -s run || exit 1' -e c",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Harrison Cross <code@crosshj.com> (https://crosshj.com)",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "1.19.4"
  }
}
